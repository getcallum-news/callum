# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
alembic==1.14.1

# HTTP & RSS parsing
httpx==0.28.1
feedparser==6.0.11

# Background scheduling
apscheduler==3.10.4

# Environment & configuration
python-dotenv==1.0.1

# Rate limiting
slowapi==0.1.9

# Input sanitization
bleach==6.2.0

# Push notifications
pywebpush==2.0.1

# Error monitoring
sentry-sdk[fastapi]==2.19.2

# Sentiment analysis
nltk==3.9.1

# Pydantic (bundled with FastAPI, pinned for clarity)
pydantic==2.10.4

# Topic modeling — BERTopic pipeline
# sentence-transformers: embeds articles into 384-dim semantic vectors
# bertopic: UMAP + HDBSCAN + c-TF-IDF topic discovery
# umap-learn / hdbscan: dimensionality reduction + density clustering
sentence-transformers>=2.7.0
bertopic>=0.16.0
umap-learn>=0.5.4
hdbscan>=0.8.33
scikit-learn>=1.3.0
